import { Box, Typography } from '@mui/material';
import React from 'react';

const <%= pageName %>Page: React.FC = () => (
  <Box sx={{ p: 3 }}>
    <Typography variant="h4"><%= pageName %> Page</Typography>
  </Box>
);

export default <%= pageName %>Page;
