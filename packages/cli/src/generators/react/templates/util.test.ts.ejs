import { <%= name %> } from './<%= name %>';

describe('<%= name %>', () => {
  it('should work correctly', () => {
    const result = <%= name %>('test');
    expect(result).toBeDefined();
  });
});
