import { Binding, Component, ProviderMap } from '@loopback/core';
export declare class RequestComponent implements Component {
    constructor();
    providers?: ProviderMap;
    bindings?: Binding[];
}
