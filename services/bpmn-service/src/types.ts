/**
 * Interface defining the component's options object
 */
export interface BpmnServiceComponentOptions {
  url?: string,
  host?: string,
  port?: number,
  user?: string,
  password?: string,
  database?: string,
  schema?: string
}
