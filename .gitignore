node_modules
.idea
.env
lerna-debug.log
**/*.bak
**/.DS_Store
.nyc_output
dist

# site contains compiled html files, we don't need them because mkdocs-deploy
# can freshly recreate them
/site

# these paths are typedoc owned
/docs/packages/
/docs/services/