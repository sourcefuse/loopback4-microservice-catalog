{
  "name": "lambda-example",
  "version": "0.0.1",
  "description": "lambda-service",
  "keywords": [
    "loopback-application",
    "loopback"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=10.16"
  },
  "scripts": {
    "build": "lb-tsc",
    "build:watch": "lb-tsc --watch",
    "lint": "npm run eslint && npm run prettier:check",
    "lint:fix": "npm run eslint:fix && npm run prettier:fix",
    "prettier:cli": "lb-prettier \"**/*.ts\" \"**/*.js\"",
    "prettier:check": "npm run prettier:cli -- -l",
    "prettier:fix": "npm run prettier:cli -- --write",
    "eslint": "lb-eslint --report-unused-disable-directives .",
    "eslint:fix": "npm run eslint -- --fix",
    "pretest": "npm run rebuild",
    "test": "lb-mocha --allow-console-logs \"dist/__tests__\"",
    "posttest": "npm run lint",
    "test:dev": "lb-mocha --allow-console-logs dist/__tests__/**/*.js && npm run posttest",
    "docker:build": "docker build -t lambda-service .",
    "docker:run": "docker run -p 3000:3000 -d lambda-service",
    "premigrate": "npm run build",
    "migrate": "node ./dist/migrate",
    "preopenapi-spec": "npm run build",
    "openapi-spec": "node ./dist/openapi-spec",
    "prestart": "npm run rebuild",
    "start": "node -r source-map-support/register .",
    "clean": "lb-clean dist *.tsbuildinfo .eslintcache",
    "rebuild": "npm run clean && npm run build",
    "lambda:create": "npm run build && node -r dotenv/config ./node_modules/.bin/claudia create --handler dist.handler --deploy-proxy-api",
    "lambda:update": "npm run build && node -r dotenv/config ./node_modules/.bin/claudia update --handler dist.handler --deploy-proxy-api"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": " <akshat.dubey@sourcefuse.com>",
  "license": "",
  "files": [
    "README.md",
    "dist",
    "src",
    "!*/__tests__"
  ],
  "dependencies": {
    "@loopback/boot": "^3.4.2",
    "@loopback/core": "^2.16.2",
    "@loopback/repository": "^3.7.1",
    "@loopback/rest": "^10.0.0",
    "@loopback/rest-explorer": "^3.3.2",
    "@loopback/service-proxy": "^3.2.2",
    "aws-lambda": "^1.0.6",
    "aws-serverless-express": "^3.4.0",
    "dotenv": "^10.0.0",
    "tslib": "^2.0.0"
  },
  "devDependencies": {
    "@loopback/build": "^7.0.0",
    "@loopback/eslint-config": "^11.0.0",
    "@loopback/testlab": "^3.4.2",
    "@types/aws-lambda": "^8.10.83",
    "@types/aws-serverless-express": "^3.3.5",
    "@types/node": "^10.17.60",
    "claudia": "^5.13.1",
    "dotenv": "^10.0.0",
    "eslint": "^7.29.0",
    "source-map-support": "^0.5.19",
    "typescript": "~4.3.4"
  }
}
