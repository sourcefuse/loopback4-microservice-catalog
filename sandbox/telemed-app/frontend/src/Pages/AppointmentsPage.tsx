import { StaticPages } from "../Components/StaticPages";

export const AppointmentsPage = () => {
  const content =
    "Manage your scheduled appointments, view upcoming consultations, and easily schedule new appointments";

  return <StaticPages content={content} />;
};
