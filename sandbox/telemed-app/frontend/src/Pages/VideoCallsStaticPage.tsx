import { StaticPages } from "../Components/StaticPages";

export const VideoCallsStaticPage = () => {
  const content = "All your appointments video call history can be seen here";

  return <StaticPages content={content} />;
};
