import { StaticPages } from "../Components/StaticPages";

export const MessagesPage = () => {
  const content = "All your chat history can be seen here";

  return <StaticPages content={content} />;
};
