import { StaticPages } from "../Components/StaticPages";

export const PrescriptionsPage = () => {
  const content =
    "Easily manage and issue digital prescriptions. Access medication history, prescribe medications accurately";

  return <StaticPages content={content} />;
};
